<p style="text-align: center; font-size: 1.2rem">
  Clique no botão a seguir para ativar sua conta:
</p>
<div style="margin-top: 1rem; text-align: center">
  <a
    href="<%= linkAtivacao %>"
    style="
      appearance: button;
      backface-visibility: hidden;
      background-color: #28ff52;
      border-radius: 6px;
      border-width: 0;
      box-shadow:
        rgba(50, 50, 93, 0.1) 0 0 0 1px inset,
        rgba(50, 50, 93, 0.1) 0 2px 5px 0,
        rgba(0, 0, 0, 0.07) 0 1px 1px 0;
      box-sizing: border-box;
      color: #fff;
      cursor: pointer;
      display: inline-block; /* Para garantir que o link se comporte como um botão */
      font-family: -apple-system, system-ui, 'Segoe UI', Roboto,
        'Helvetica Neue', Ubuntu, sans-serif;
      font-size: 100%;
      height: 80px;
      line-height: 44px; /* Ajusta o alinhamento vertical do texto */
      margin: 12px 0 0;
      outline: none;
      overflow: hidden;
      padding: 0 25px;
      position: relative;
      text-align: center;
      text-decoration: none; /* Remove o sublinhado padrão do link */
      text-transform: none;
      transform: translateZ(0);
      transition:
        all 0.2s,
        box-shadow 0.08s ease-in;
      user-select: none;
      -webkit-user-select: none;
      touch-action: manipulation;
      width: auto; /* Ajusta a largura do botão */
    "
  >
    Ativar Conta
  </a>
</div>
